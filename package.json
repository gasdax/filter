{
  "name": "student-lottery-demo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm:dev:server\" \"npm:dev:client\"",
    "dev:client": "vite --host 0.0.0.0 --port 4173 --strictPort",
    "dev:server": "node server.mjs",
    "test": "vitest run --environment jsdom",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@testing-library/dom": "^10.4.1",
    "concurrently": "^9.2.1",
    "jsdom": "^28.1.0",
    "typescript": "~5.9.3",
    "vite": "^8.0.1",
    "vitest": "^4.1.0"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
